# Service and Deployment configuration for Policy service deployment.
##################################################################################################
# Policy api
##################################################################################################
apiVersion: v1
kind: ConfigMap
metadata:
  name: policy-api-vs
data:
  spring.application.name: policy-api-vs
  external.services.customer.uri: http://customer-api-vs:8080
  spring.data.mongodb.uri: mongodb+srv://demo123:demo123@acctrainings.dntd4.mongodb.net/db-acc-trainings?retryWrites=true&w=majority
  spring.data.mongodb.database: db-acc-trainings
  my.path: /api/vs
  springdoc.swagger-ui.path: /api/vs/policy-api/swagger-ui.html
  springdoc.swagger-ui.operationsSorter: method
---
apiVersion: v1
kind: Service
metadata:
  name: policy-api-vs
  labels:
    app: policy-api-vs
    service: policy-api-vs
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: policy-api-vs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-api-release-1.0.0
  labels:
    app: policy-api-vs
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: policy-api-vs
      version: v1
  template:
    metadata:
      labels:
        app: policy-api-vs
        version: v1
    spec:
      containers:
      - name: policy-api-vs
        image: quay.io/acctrainings/policy-api-gold:release-1.0.0
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: policy-api-vs
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Route
metadata:
  name: policy-api-vs
  labels:
    app: policy-api-vs
spec:
  to:
    kind: Service
    name: policy-api-vs
